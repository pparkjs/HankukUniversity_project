<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="kr.or.hku.login.mapper.LoginMapper">
 	<select id="loginUser" parameterType="usersVO" resultType="usersVO">
 		select * from users
 		where user_no = #{userNo} and user_pw = #{userPw} and user_cls_cd = #{userClsCd}
 	</select>
 
 	<select id="studentUser" parameterType="String" resultType="studentVO">
 		select * from student
 		where std_no = #{stdNo}
 	</select>
 	
 	<select id="professorUser" parameterType="String" resultType="professorVO">
 		select PRO_NO,
				b.DEPT_NM as DEPT_CD,
				PRO_JBTTL_CD,
				PRO_JNCMP_YMD,
				PRO_RSGNTN_YMD,
				PRO_LAB,
				PRO_LAB_TELNO,
				PRO_NM,
				PRO_TELNO,
				SEX_CD,
				PRO_ZIP,
				PRO_ADDR,
				PRO_DADDR,
				PRO_MAIL,
				PRO_BRDT,
				PRO_RRNO,
				BANK_CD,
				PRO_ACTNO,
				PRO_PROFILE_PATH
				from professor a
				join department b on a.dept_cd = b.dept_cd
 		where pro_no = #{proNo}
 	</select>
 	
 	 <select id="employeeUser" parameterType="String" resultType="employeeVO">
 		select * from employee
 		where emp_no = #{empNo}
 	</select>
 	
 </mapper>